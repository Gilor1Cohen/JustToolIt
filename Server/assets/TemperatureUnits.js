const table = {
  C: {
    C: (v) => v,
    F: (v) => (v * 9) / 5 + 32,
    K: (v) => v + 273.15,
  },
  F: {
    C: (v) => ((v - 32) * 5) / 9,
    F: (v) => v,
    K: (v) => ((v - 32) * 5) / 9 + 273.15,
  },
  K: {
    C: (v) => v - 273.15,
    F: (v) => ((v - 273.15) * 9) / 5 + 32,
    K: (v) => v,
  },
};

module.exports = table;
